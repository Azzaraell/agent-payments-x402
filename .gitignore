# Secrets — NEVER commit. Agent wallets and keypairs live here.
.env
.env.*
!.env.example
*.key
*-keypair.json
keypair.json
id.json
wallet.json

# Node
node_modules/
dist/
build/
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log

# Local agent guide — kept on disk, not part of the skill payload
CLAUDE.md
